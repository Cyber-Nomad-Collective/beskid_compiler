//! Default implementation for TreeNavigator.

use crate::primitives::tree_view::tree_navigator::TreeNavigator;

impl Default for TreeNavigator {
    fn default() -> Self {
        Self::new()
    }
}
