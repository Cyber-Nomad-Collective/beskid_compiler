/// Fiber handle runtime tests.
use Concurrency.Fiber;
use Testing.Assert;

test fiber_module_compiles {
    return;
}

test fiber_spawn_join_api_smoke {
    Assert.True(true, "Fiber spawn/join API compiles");
}
