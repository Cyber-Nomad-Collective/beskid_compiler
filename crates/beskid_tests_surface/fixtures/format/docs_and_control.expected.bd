/// Utility module docs
pub mod Util
{
    /// Checks if value is positive
    pub bool IsPositive(i32 value)
    {
        if (value > 0)
        {
            return true;
        }
        else
        {
            return false;
        }
    }
}

pub unit Run()
{
    while (cond)
    {
        break;
    }

    let x = range(0, 10);
}
