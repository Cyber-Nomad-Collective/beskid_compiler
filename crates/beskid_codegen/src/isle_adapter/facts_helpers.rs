//! Inherent `SyntaxNodeFacts` helpers shared by the `NodeFacts` implementation, grouped by fact.

mod array_layout;
mod call_context;
mod enum_layout;
mod node_walk;
mod struct_layout;
mod type_dispatch;
