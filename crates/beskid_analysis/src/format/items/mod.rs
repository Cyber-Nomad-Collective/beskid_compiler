mod attributes_emit;
mod declarations_emit;
mod functions_emit;
mod helpers;
mod meta_definition_emit;
mod root_emit;
mod tests_emit;

// Orphan `Emit` impls live in submodules; this module exists to wire the formatter together.
