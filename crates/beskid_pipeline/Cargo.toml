[package]
name = "beskid_pipeline"
version = "0.1.0"
edition = "2024"
license.workspace = true
description = "Shared compilation pipeline phase ids, events, and observers (std-only)"

[dependencies]
tracing = { workspace = true, features = ["std"] }
