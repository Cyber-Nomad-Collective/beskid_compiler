/// Query.Iterator — contract-based iteration for Beskid collections.
/// @tier(supported)
/// Modeled after Rust's `Iterator` trait. Concrete iterators implement this contract.
/// Marker contract for iterator implementations.
///
/// Element-typed iteration is expressed by concrete generic iterators until contracts gain
/// generic parameters in the language grammar.
pub contract Iterator {
}
