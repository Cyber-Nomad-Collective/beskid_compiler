[package]
name = "beskid_abi"
version = "0.1.0"
edition = "2024"

[dependencies]
serde = { version = "1", features = ["derive"] }
sha2 = "0.10"

[dev-dependencies]
serde_json = "1"

[build-dependencies]
beskid_manifest = { path = "../beskid_manifest" }
