[package]
name = "beskid_tests_interop"
version = "0.1.0"
edition = "2024"

[features]
default = []
slow = []

[dependencies]
beskid_aot = { path = "../beskid_aot" }
beskid_engine = { path = "../beskid_engine" }
