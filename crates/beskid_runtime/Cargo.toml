[package]
name = "beskid_runtime"
version = "0.1.0"
edition = "2024"

[dependencies]
beskid_abi = { path = "../beskid_abi" }
abfall = { path = "../abfall" }
slotmap = { workspace = true }
corosensei = { version = "0.3.3", default-features = true }

[dev-dependencies]
criterion = "0.5"

[features]
metrics = []
arrays_backing = []
sched = []

[[bench]]
name = "runtime_micro"
harness = false
