[build]
# sccache is wired automatically. Release/delivery jobs may override by unsetting RUSTC_WRAPPER.
rustc-wrapper = "sccache"

# Shared target dir across git worktrees (set per-worktree via env if you prefer isolation).
# Uncomment to share: CARGO_TARGET_DIR is not a config key; document in justfile instead.

[target.aarch64-apple-darwin]
# rust-lld ships with the toolchain and is the safe darwin linker. Use "mold" only if validated.
linker = "rust-lld"

[target.x86_64-pc-windows-msvc]
linker = "rust-lld.exe"
